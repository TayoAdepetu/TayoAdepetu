# Optional — enable Resend in app/api/contact/route.ts to send enquiry emails.
# RESEND_API_KEY=
# CONTACT_TO_EMAIL=

# Platform-managed (do not set manually when deployed on LaunchWithCursor):
# PORT, NODE_ENV — injected for every service
# EMAIL_API_URL, EMAIL_API_KEY, EMAIL_FROM_DOMAIN — after Deploy → Email → Enable
